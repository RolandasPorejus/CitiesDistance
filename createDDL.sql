CREATE TABLE CITY (ID NUMBER(10) NOT NULL, COUNTRY VARCHAR, NAME VARCHAR, PLACEID VARCHAR, PRIMARY KEY (ID))
CREATE TABLE BOUNDS (ID NUMBER(10) NOT NULL, NORTH_EAST NUMBER(10), SOUTH_WEST NUMBER(10), CITY_ID NUMBER(10), PRIMARY KEY (ID))
CREATE TABLE COORDINATES (ID NUMBER(10) NOT NULL, LATITUDE NUMBER(10,5), LONGITUDE NUMBER(10,5), BOUNDS_ID NUMBER(10), PRIMARY KEY (ID))
CREATE TABLE LOCATION (ID NUMBER(10) NOT NULL, LATITUDE NUMBER(10,5), LONGITUDE NUMBER(10,5), CITY_ID NUMBER(10), PRIMARY KEY (ID))
ALTER TABLE BOUNDS ADD CONSTRAINT FK_BOUNDS_CITY_ID FOREIGN KEY (CITY_ID) REFERENCES CITY (ID)
ALTER TABLE COORDINATES ADD CONSTRAINT FK_COORDINATES_BOUNDS_ID FOREIGN KEY (BOUNDS_ID) REFERENCES BOUNDS (ID)
ALTER TABLE LOCATION ADD CONSTRAINT FK_LOCATION_CITY_ID FOREIGN KEY (CITY_ID) REFERENCES CITY (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT NUMBER(19), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
